spring:
  application:
    name: user-service
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

user.greeting: Hello Cloud
