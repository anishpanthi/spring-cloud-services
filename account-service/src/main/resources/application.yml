spring:
  application:
    name: account-service
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

account.greeting: Hello Cloud
